[package]
name = "pmm-abi"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { optional = true, default-features = false, version = "1.0", features = ["derive"] }
stabby = { version = "36.2", features = ["serde"], git = "https://github.com/slbsh/stabby" }

[features]
serde = ["dep:serde"]
