name = "pacman"
strat = "artix"
env."args" = "--color=always"

[impl]
add    = "pacman ${args} -S ${items}"
remove = "pacman ${args} -Rns ${items}"
query  = "pacman ${args} -Qi ${items}"
list   = "pacman ${args} -Ssq ${items}"
